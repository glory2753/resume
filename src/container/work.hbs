<main>
  {{#with main}}
      <section>
        {{> title data="Work history" big=false}}
        <div>
          <ul>
            {{#each works}}
              <li>
                <div>{{position}}</div>
                <br>
                <div>
                  {{duration.from}}
                  -
                  {{#if duration.to}}
                    {{duration.to}}
                  {{else}}
                    (досі працюємо)
                  {{/if}}
                </div>
                <br>
                <br>
                {{#if company}}
                  {{#if company.url}}
                    <div>{{> link text=company.name href=company.url blank=true noLine=false}}</div>
                  {{else}}
                    <div><b>{{company.name}}</b></div>
                  {{/if}}
                {{/if}}
                <br>
                <br>
                <div>
                  <h3 class="ms-0">Projects ({{projectAmount}})</h3>
                </div>
                <br>
                <ul>
                  {{#each projects}}
                    <li>
                      <div>{{> link text.name href=url blank=true noLine=false}}</div>
                      <div>
                        <b>{{about}}</b>
                      </div>
                      <br>
                      <div>
                        <em>Project stacks ({{stackAmount}})</em>
                        <ul>
                          {{#each stacks}}
                            <li>{{name}}</li>
                          {{/each}}
                        </ul>
                      </div>
                      <br>
                      <div>
                        <em>Project awards</em>
                        <ul>
                          {{#each awards}}
                            <li>{{name}}</li>
                          {{/each}}
                        </ul>
                      </div>
                    </li>
                    <br>
                  {{/each}}
                </ul>
              </li>
            {{/each}}
          </ul>
        </div>
      </section>
      {{/with}}
    </main>
  

